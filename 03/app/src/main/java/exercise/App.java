package exercise;


/**
 * Represents the main application for Exercise 03.
 * Initializes a button and adds an action listener to it.
 * It then asks the user to press the button.
 * <p>
 * This class demonstrates basic user interaction and event handling
 * using custom button and listener implementations.
 * </p>
 *
 */
public final class App {
    /**
     * Private constructor to prevent instantiation of utility class.
     */
    private App() {
        // Utility class, no instantiation required
    }

    /**
     * The main method that serves as the entry point of the application.
     *
     * @param args Command line arguments passed to the application (not used).
     */
    public static void main(final String[] args) {

    }
}
