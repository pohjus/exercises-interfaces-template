package exercise;

/**
 * The Callback interface defines a callback method to handle content.
 */
public interface Callback {
    /**
     * Callback method to handle content.
     *
     * @param content The content to be processed.
     */
    void callback(String content);
}
